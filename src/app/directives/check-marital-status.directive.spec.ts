import { CheckMaritalStatusDirective } from './check-marital-status.directive';

describe('CheckMaritalStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckMaritalStatusDirective();
    expect(directive).toBeTruthy();
  });
});

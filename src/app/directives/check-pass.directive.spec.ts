import { CheckPassDirective } from './check-pass.directive';

describe('CheckPassDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckPassDirective();
    expect(directive).toBeTruthy();
  });
});
